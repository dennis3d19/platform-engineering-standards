# Example fixture: valid Kubernetes namespace YAML
# Used in tests to verify YAML validation logic
apiVersion: v1
kind: Namespace
metadata:
  name: test-fixture
  labels:
    team: platform
    environment: tst
    managed-by: test
