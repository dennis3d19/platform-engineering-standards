# Test environment overrides
replicaCount: 1

config:
  logLevel: debug
  environment: tst

autoscaling:
  enabled: false

podDisruptionBudget:
  enabled: false

monitoring:
  enabled: true
  serviceMonitor:
    enabled: true
